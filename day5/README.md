## Using kotlin

`kotlinc-native main.kt -o build/main.exe`
or
`kotlinc main.kt -include-runtime -kotlin-home "%KOTLIN_HOME%" -d build/main.jar`
then `java -jar build/main.jar`

Never used kotlin before but I can see how it's built around using a Jetbrains IDE.

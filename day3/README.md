# To build

Just using the following
`clang "src/parser.c" "src/part1.c" "src/main.c" -o build/day3.exe`
since alternatives are much more painful

I tried cmake but it was a bit agonising

For testing run
`clang "src/parser.c" "src/part1.c" "test/test.c" -o build/test.exe`

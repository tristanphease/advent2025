Thought process:

- First, need a parser
- To do this, can go to https://lib.rs/parsing for a good list
- Will use nom, it's popular, fast and I haven't used it before

To run:
Add input file in
Run `cargo run -- input.txt` for part 1
Run `cargo run -- input.txt 2` for part 2
